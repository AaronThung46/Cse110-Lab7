1. You run the automated tests within a Github action that runs whenever code is pushed. This is done as a safety net. Any time changes are being made to the code you want to ensure that their aren't any unintended issues or broken features. 

2. No, if you want to test a specific function you would want to use a unit test. 

3. In Navigation Mode the information analyzed is when the page is just loaded. Snapshot Mode analyzes the page at its current state. 
   
4. Adding the lang attribute is one fixes. The second fix adding a meta descrption in the <head> section. Another fix is adding defer attribute to the scripts tags in the HTML file. 


